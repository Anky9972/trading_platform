smartapi-python>=1.3.0
pyotp>=2.8.0
pandas>=2.0.0
cryptography>=41.0.0
yfinance>=0.2.0

# alpha_factory (research) — P0 modules use stdlib only.
# Uncomment as you reach later phases:
#requests>=2.31.0     # BRAIN HTTP client (already a transitive dep)
#pytest>=8.0.0        # for research/alpha_factory/tests/
# P1+ (LLM personas, local sim):
#polars>=0.20.0
#pydantic>=2.5.0
#jinja2>=3.1.0
#aiohttp>=3.9.0
# P2+ (LLM + RAG):
#outlines>=0.0.40
#chromadb>=0.4.0
#arxiv>=2.1.0
